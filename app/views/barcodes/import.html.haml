=form_tag({:action => :upload}, :multipart => true) do
  =file_field_tag :excel
  =submit_tag "Submit", class: "btn btn-primary submit"
  =link_to "Cancel", "/barcodes", class: "btn btn-primary submit"